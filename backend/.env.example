# Database Configuration
DATABASE_URL=postgres://postgres:tech_office_password@localhost:15432/tech_office_db?sslmode=disable
FLOW_SHARD_COUNT=1
POSTGRES_DB=tech_office_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tech_office_password
WEBAPP_URL=http://localhost:13000

# Runtime profile: development or production. Unset means production, because the safe
# posture has to be the one you get by not thinking about it — so a contributor's machine
# has to say so out loud. In development the startup safety checks are logged as warnings
# and every browser origin is permitted; in production a failing check refuses to start.
# `make voice-dev-backend` sets this for you.
APP_ENV=development

# AWS SES Configuration (transactional email)
# If SES_FROM_EMAIL is empty, password reset and invitation emails are logged locally instead.
# AWS credentials can come from AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY or an attached IAM role.
AWS_REGION=
SES_FROM_EMAIL=
SES_REPLY_TO_EMAIL=
SES_CONFIGURATION_SET=

# Internal JWT Configuration
# Private key for signing JWTs (public key derived automatically)
# If not set, an ephemeral key is generated — tokens do not survive a restart, and the
# production profile refuses to start rather than do this.
JWT_PRIVATE_KEY_PATH=.dev-keys/jwt-private.pem

# Firebase Cloud Messaging Configuration (T054)
# Option 1: Path to service account JSON file (preferred for local dev)
GOOGLE_APPLICATION_CREDENTIALS=.dev-keys/techoffice-290fa-114ba9180b77.json

# Option 2: Inline JSON string (alternative for environments without filesystem access)
# FCM_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"..."}

# Optional: Explicit project ID (can be derived from service account)
# FCM_PROJECT_ID=your-firebase-project-id

R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT=
# Optional custom domain (needs the Cloudflare WAF token-auth rule, see deploy/.env.example).
R2_PUBLIC_URL=
R2_PUBLIC_URL_HMAC_SECRET=

# LiveKit Voice Configuration
# PUBLIC_LIVEKIT_URL is the URL sent to clients in join credentials.
# Do not pin it here: `make voice-dev-backend` and `make test-backend` derive it from
# this machine's current LAN IP (backend/scripts/dev/voice-env.sh), and a pinned value
# rots the next time the machine changes network. Override with TECH_OFFICE_HOST_IP.
# LIVEKIT_URL=ws://localhost:7880
# PUBLIC_LIVEKIT_URL=
# LIVEKIT_KEYS=devkey: devsecretdevsecretdevsecretdevsecret
# LIVEKIT_API_KEY=devkey
# LIVEKIT_API_SECRET=devsecretdevsecretdevsecretdevsecret