apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: livekit-component

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      component: livekit