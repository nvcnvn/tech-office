apiVersion: v1
kind: Secret
metadata:
  name: fcm-service-account
  labels:
    app.kubernetes.io/name: fcm-service-account
    app.kubernetes.io/part-of: tech-office
    app.kubernetes.io/component: notification
type: Opaque
stringData:
  # Replace the placeholder JSON below with your Firebase service account.
  # Never commit the real credentials to source control.
  serviceAccount.json: |-
    {
      "replace": "with-real-service-account"
    }
