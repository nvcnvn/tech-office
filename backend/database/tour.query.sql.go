// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: tour.query.sql

package database

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
	"github.com/nvcnvn/tech-office/backend/database/dbuuid"
)

const deleteTourProgressForOrganization = `-- name: DeleteTourProgressForOrganization :exec
DELETE FROM iam.tour_progress
WHERE organization_id = $1 AND employee_id = $2
`

type DeleteTourProgressForOrganizationParams struct {
	OrganizationID dbuuid.UUID `json:"organization_id"`
	EmployeeID     dbuuid.UUID `json:"employee_id"`
}

// Remove a person's tour progress within one organization. Called from the account
// deletion sweep in internal/iam/logic_account_deletion.go, alongside the equivalent
// user-preference delete: the sweep is explicit rather than cascade-dependent so it
// stays idempotent on a retry.
//
// Parameters:
// - $1 organization_id (UUID, required): Tenant isolation
// - $2 employee_id (UUID, required): The person
func (q *Queries) DeleteTourProgressForOrganization(ctx context.Context, db DBTX, arg *DeleteTourProgressForOrganizationParams) error {
	_, err := db.Exec(ctx, deleteTourProgressForOrganization, arg.OrganizationID, arg.EmployeeID)
	return err
}

const getTourProgress = `-- name: GetTourProgress :one

SELECT id, organization_id, employee_id, tour_id, status, current_stop, content_version, updated_at FROM iam.tour_progress
WHERE organization_id = $1 AND employee_id = $2 AND tour_id = $3
LIMIT 1
`

type GetTourProgressParams struct {
	OrganizationID dbuuid.UUID `json:"organization_id"`
	EmployeeID     dbuuid.UUID `json:"employee_id"`
	TourID         string      `json:"tour_id"`
}

// ===============================================
// Feature Tour Queries (Feature 039)
//
// Every query pins organization_id to a parameter. None is cross-tenant, so none carries
// a -- lint:cross-tenant marker.
// ===============================================
// Read one person's progress in one tour.
// Performance: O(1) via the unique index on (organization_id, employee_id, tour_id).
//
// Parameters:
// - $1 organization_id (UUID, required): Tenant isolation
// - $2 employee_id (UUID, required): The person
// - $3 tour_id (text, required): "administrator" or "worker"
func (q *Queries) GetTourProgress(ctx context.Context, db DBTX, arg *GetTourProgressParams) (*IamTourProgress, error) {
	row := db.QueryRow(ctx, getTourProgress, arg.OrganizationID, arg.EmployeeID, arg.TourID)
	var i IamTourProgress
	err := row.Scan(
		&i.ID,
		&i.OrganizationID,
		&i.EmployeeID,
		&i.TourID,
		&i.Status,
		&i.CurrentStop,
		&i.ContentVersion,
		&i.UpdatedAt,
	)
	return &i, err
}

const upsertTourProgress = `-- name: UpsertTourProgress :one
INSERT INTO iam.tour_progress (
    id,
    organization_id,
    employee_id,
    tour_id,
    status,
    current_stop,
    content_version,
    updated_at
) VALUES (
    $1, $2, $3, $4, $5, $6, $7, $8
) ON CONFLICT (organization_id, employee_id, tour_id) DO UPDATE SET
    status = EXCLUDED.status,
    current_stop = EXCLUDED.current_stop,
    content_version = EXCLUDED.content_version,
    updated_at = EXCLUDED.updated_at
RETURNING id, organization_id, employee_id, tour_id, status, current_stop, content_version, updated_at
`

type UpsertTourProgressParams struct {
	ID             dbuuid.UUID        `json:"id"`
	OrganizationID dbuuid.UUID        `json:"organization_id"`
	EmployeeID     dbuuid.UUID        `json:"employee_id"`
	TourID         string             `json:"tour_id"`
	Status         string             `json:"status"`
	CurrentStop    int32              `json:"current_stop"`
	ContentVersion string             `json:"content_version"`
	UpdatedAt      pgtype.Timestamptz `json:"updated_at"`
}

// Insert or update progress on the natural key. Re-sending the same stop index is a
// no-op beyond updated_at, which matters because both clients may write on navigation
// and again on unmount.
// Takes updated_at as a parameter so the caller controls the timestamp.
//
// Parameters:
// - $1 id (UUID): Record ID (uuidv7); ignored on conflict
// - $2 organization_id (UUID, required): Tenant isolation
// - $3 employee_id (UUID, required): The person
// - $4 tour_id (text, required): "administrator" or "worker"
// - $5 status (text, required): "in_progress", "completed" or "dismissed"
// - $6 current_stop (int, required): Zero-based index into the filtered stop list
// - $7 content_version (text, required): The content version this person saw
// - $8 updated_at (timestamptz): Parameterized timestamp
func (q *Queries) UpsertTourProgress(ctx context.Context, db DBTX, arg *UpsertTourProgressParams) (*IamTourProgress, error) {
	row := db.QueryRow(ctx, upsertTourProgress,
		arg.ID,
		arg.OrganizationID,
		arg.EmployeeID,
		arg.TourID,
		arg.Status,
		arg.CurrentStop,
		arg.ContentVersion,
		arg.UpdatedAt,
	)
	var i IamTourProgress
	err := row.Scan(
		&i.ID,
		&i.OrganizationID,
		&i.EmployeeID,
		&i.TourID,
		&i.Status,
		&i.CurrentStop,
		&i.ContentVersion,
		&i.UpdatedAt,
	)
	return &i, err
}
