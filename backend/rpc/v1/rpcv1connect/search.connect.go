// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: rpc/v1/search.proto

package rpcv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/nvcnvn/tech-office/backend/rpc/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SearchServiceName is the fully-qualified name of the SearchService service.
	SearchServiceName = "rpc.v1.SearchService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SearchServiceSearchProcedure is the fully-qualified name of the SearchService's Search RPC.
	SearchServiceSearchProcedure = "/rpc.v1.SearchService/Search"
)

// SearchServiceClient is a client for the rpc.v1.SearchService service.
type SearchServiceClient interface {
	// Search fans out over every source the caller is permitted to search and returns one
	// ranked list plus a per-source outcome report.
	//
	// AUTHORIZATION. Empty required_permissions with allow_unauthenticated: false is the
	// deliberate "any authenticated caller" choice, not an omission (Constitution III).
	// It has to be: each of the eight sources has its own permission, and declaring any one
	// of them here would deny the whole search to somebody entitled to the other seven.
	// The connect layer reads the caller's effective permissions and reports each source it
	// may not search as SOURCE_STATUS_NOT_PERMITTED — a skip, not a failure (FR-012).
	//
	// FAILURE. A source that errors or misses its deadline is reported UNAVAILABLE and the
	// search still returns everything else (FR-005). The only error response is when no
	// source answered at all, which is Unavailable — an empty list would tell the caller
	// their workspace is empty (FR-004, US3).
	Search(context.Context, *connect.Request[v1.SearchRequest]) (*connect.Response[v1.SearchResponse], error)
}

// NewSearchServiceClient constructs a client for the rpc.v1.SearchService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSearchServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SearchServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	searchServiceMethods := v1.File_rpc_v1_search_proto.Services().ByName("SearchService").Methods()
	return &searchServiceClient{
		search: connect.NewClient[v1.SearchRequest, v1.SearchResponse](
			httpClient,
			baseURL+SearchServiceSearchProcedure,
			connect.WithSchema(searchServiceMethods.ByName("Search")),
			connect.WithClientOptions(opts...),
		),
	}
}

// searchServiceClient implements SearchServiceClient.
type searchServiceClient struct {
	search *connect.Client[v1.SearchRequest, v1.SearchResponse]
}

// Search calls rpc.v1.SearchService.Search.
func (c *searchServiceClient) Search(ctx context.Context, req *connect.Request[v1.SearchRequest]) (*connect.Response[v1.SearchResponse], error) {
	return c.search.CallUnary(ctx, req)
}

// SearchServiceHandler is an implementation of the rpc.v1.SearchService service.
type SearchServiceHandler interface {
	// Search fans out over every source the caller is permitted to search and returns one
	// ranked list plus a per-source outcome report.
	//
	// AUTHORIZATION. Empty required_permissions with allow_unauthenticated: false is the
	// deliberate "any authenticated caller" choice, not an omission (Constitution III).
	// It has to be: each of the eight sources has its own permission, and declaring any one
	// of them here would deny the whole search to somebody entitled to the other seven.
	// The connect layer reads the caller's effective permissions and reports each source it
	// may not search as SOURCE_STATUS_NOT_PERMITTED — a skip, not a failure (FR-012).
	//
	// FAILURE. A source that errors or misses its deadline is reported UNAVAILABLE and the
	// search still returns everything else (FR-005). The only error response is when no
	// source answered at all, which is Unavailable — an empty list would tell the caller
	// their workspace is empty (FR-004, US3).
	Search(context.Context, *connect.Request[v1.SearchRequest]) (*connect.Response[v1.SearchResponse], error)
}

// NewSearchServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSearchServiceHandler(svc SearchServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	searchServiceMethods := v1.File_rpc_v1_search_proto.Services().ByName("SearchService").Methods()
	searchServiceSearchHandler := connect.NewUnaryHandler(
		SearchServiceSearchProcedure,
		svc.Search,
		connect.WithSchema(searchServiceMethods.ByName("Search")),
		connect.WithHandlerOptions(opts...),
	)
	return "/rpc.v1.SearchService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SearchServiceSearchProcedure:
			searchServiceSearchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSearchServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSearchServiceHandler struct{}

func (UnimplementedSearchServiceHandler) Search(context.Context, *connect.Request[v1.SearchRequest]) (*connect.Response[v1.SearchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("rpc.v1.SearchService.Search is not implemented"))
}
