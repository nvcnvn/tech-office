# Domain Snapshots

**Status date: 2026-09-05** · Branch at capture: `052-unreachable-callee-missed-call`

These documents describe **what the system does today**, domain by domain, derived by
reading the code, the proto contracts and `backend/database/scripts/schema.sql` — not by
replaying the feature specs. Read one of these instead of walking `specs/001` → `specs/035`.

## Why this exists

`specs/NNN-*` are *incremental change proposals*, written before implementation. Two
problems follow from that:

1. **They are cumulative, not current.** Understanding "how do rituals work" from specs
   alone means reading 022, 023, 028, 029 and 034 and mentally applying each diff.
2. **Some are stale.** Work landed outside the Spec Kit workflow, and some specs describe
   a design that was later replaced (024 is titled "passkey" but shipped as PIN).

The rule going forward: **specs remain the record of intent; these documents are the
record of behaviour.** Where they disagree, the code wins and the drift is recorded here.

## Index

| Document | Covers | Backing code |
|---|---|---|
| [platform.md](platform.md) | Architecture tiers, multi-tenancy, Connect-RPC, permission enforcement, background jobs, config, testing | `backend/cmd`, `backend/internal/interceptor`, `backend/database` |
| [auth-identity.md](auth-identity.md) | Sign-up, sign-in, SSO, PIN accounts, sessions, invitations, roles & permissions | `internal/iam`, `rpc/v1/iam.proto` |
| [organization-people.md](organization-people.md) | Organizations, employees, employee import, departments, org chart | `internal/organization`, `internal/department` |
| [chat.md](chat.md) | Channels, messages, threads, reactions, typing, sidebar config, chat file uploads | `internal/chat`, `rpc/v1/chat.proto` |
| [voice.md](voice.md) | Voice calls, voice messages, recordings, transcripts, LiveKit integration | `internal/voice` |
| [notifications-presence.md](notifications-presence.md) | Notification hub, subscriptions, SSE, push, rescue push, presence ping-pong | `internal/notification` |
| [rituals-tasks.md](rituals-tasks.md) | Projects, tasks, workflow rules, ritual definitions, evidence, the generation, reconciliation and shift-resolution sweeps | `internal/collaboration` |
| [docs-knowledge.md](docs-knowledge.md) | Documents, versions, comments, embeds, collaborative editing | `internal/docs` |
| [files.md](files.md) | Upload flow, quota, virus scan & validation, access rules, PDF conversion, content index | `internal/files` |
| [calendar.md](calendar.md) | Events, recurrence, attendees, resources, booking links, delegation, check-in, shift coverage | `internal/calendar` |
| [workspace-navigation.md](workspace-navigation.md) | Federated search, canonical resource links, context rail, theme preferences, feature tour, web & mobile shells | `internal/linking`, `internal/preference`, `internal/tour`, `frontend/apps/*` |
| [compliance-safety.md](compliance-safety.md) | Content reporting, blocking, account deletion, removal requests, terms acceptance, store manifest | `internal/compliance`, `rpc/v1/compliance.proto` |

## Related documents

`backend/docs/` holds the engineering-internal architecture references that Constitution
principle XII mandates. They are narrower and deeper than these snapshots and remain
authoritative for their subjects:

- `backend/docs/SYSTEM-ARCHITECTURE.md` — tier model, dependency graphs, FK reference map
- `backend/docs/NOTIFICATION-SYSTEM-ARCHITECTURE.md` — subscription resolution, delivery pipeline
- `backend/docs/NOTIFICATION-RESCUE-PUSH-DESIGN.md`, `NOTIFICATION-RULES.md`, `FCM-SETUP.md`
- `backend/docs/VOICE-COMMUNICATION-ARCHITECTURE.md`
- `backend/docs/PRODUCTION-RUNTIME-SERVICES.md`, `PRODUCTION-DAY1-CHECKLIST.md`

## Drift register

Inconsistencies found while writing these snapshots and not yet fixed. Each is described in
full in its domain document; this is the roll-up. Rows are deleted when the underlying
problem is fixed, not annotated — the register is a list of open problems, not a changelog.

| # | Severity | Where | Summary |
|---|---|---|---|
| D16 | Contract | [organization-people.md](organization-people.md#workspace-address) | The workspace-address derivation and format rules exist twice: `internal/organization/subdomain.go` and `packages/apis/src/organization.ts`. The client copy is needed to show a derived address without a round trip, but nothing enforces that the two stay in step — `internal/organization/subdomain_test.go` is the reference table, and the TypeScript copy is verified only by having been checked against it once, by hand. |
| D17 | Contract | [auth-identity.md](auth-identity.md#3-pin-org-managed-worker-accounts) | `LoginWithPINRequest.login_identifier` now accepts an email as well as a login identifier. The field name was deliberately not changed, so the proto name understates what it takes. Clients label it "your ID or work email". |
| D18 | Contract | [workspace-navigation.md](workspace-navigation.md#known-drift) | iOS 18's automatic-strong-password cover view sits over the mobile signup password field and is outside the app's accessibility tree, so Maestro cannot drive it and `onboarding/owner-signup.yaml` cannot pass. `textContentType="password"`, explicit `autoComplete` and `passwordRules` do not suppress it; only a full AutoFill opt-out does, at the cost of password-manager fill on the owner's PIN-recovery credential. US2/US3 have backend scenario coverage but no passing blackbox flow, so Constitution XIII's per-story Maestro requirement is unmet for them. |
| D21 | Contract | [notifications-presence.md](notifications-presence.md#call-wakes), [voice.md](voice.md#native-call-presentation) | Spec 037's `contracts/call-wake-payloads.md` specifies a flat top-level call wake payload. The shipped payload uses the client module's `incomingCall` envelope instead, because `expo-callkit-telecom` parses the push and reports the call to the OS before JavaScript runs — and only recognises that shape. Field meanings are unchanged; nesting differs, and our fields ride in the module's `metadata` pass-through. Recorded in full under "Deviations recorded during implementation" in `specs/037-native-call-wakeup/plan.md`. |
| D23 | Deferred | [voice.md](voice.md#known-drift) | Spec 037's FR-021 (system recent-calls surface) is not implemented. Jetpack Telecom's unified call history and `isLogExcluded` require Android 16.1 (SDK 36.1), far above the epic's API 26 floor. FR-021 is a MAY; revisit when the 16.1 install base justifies it. |
| D24 | Environment | [voice.md](voice.md#known-drift) | `PUBLIC_LIVEKIT_URL` is derived from the current LAN IP by `backend/scripts/dev/voice-env.sh` for both the dev server and `make test-backend*`; a value pinned in a developer's gitignored `backend/.env` is ignored by the server but read by the test process, so `TestVoiceLiveKitConnectivity` fails on a URL mismatch. Treat that failure as a config problem before suspecting code. |
| D27 | Contract | [notifications-presence.md](notifications-presence.md#known-drift) | Mobile subscribes to and branches on SSE event types the backend never emits (`chat_message`, `chat_reaction`). Every real event arrives as `notification` and the reaction branch has a `notificationType` fallback, so nothing breaks, but the vocabulary reads as though a second event family exists. |
| D30 | Contract | [workspace-navigation.md](workspace-navigation.md#theme-and-preferences) | The two clients read the same stored theme contract under different rules. Mobile follows a *later* OS change while `preference_source = 'os_default'`; the web `ThemeProvider` adopts the OS scheme once on first run and never re-reads it, so a web session left on `os_default` does not move when the machine switches to dark. Deliberate, not an oversight: following a later OS change is a mobile requirement, and changing the web client was out of scope for the feature that added it. Closing it means deciding which rule both clients should follow and moving the resolver into `packages/apis` beside `theme-storage.ts`. |
| D33 | Contract | [rituals-tasks.md](rituals-tasks.md#the-channels-remembered-destination) | `ChannelDestinationUnsetReason.PROJECT_DELETED` is implemented but unreachable. `GetChannelTaskDestination` returns it when a destination row outlives its project, but `channel_task_destination`'s project foreign key is `ON DELETE CASCADE` — as `specs/038-chat-task-quick-action/data-model.md` specifies — so a deleted project takes the row with it and the channel reads `NEVER_SET` instead. The product exposes no project deletion at all (archiving is the supported operation), so the value is dead today. The approved scenario list asks for `PROJECT_DELETED`; the test asserts what actually happens and says why. |
| D34 | Deferred | [workspace-navigation.md](workspace-navigation.md#mobile-application) | Nothing runs `tsc` on mobile in CI, which is how three type errors survived for several features. All three were fixed while making mobile dark mode's static gate real — `pnpm run typecheck:mobile` now exits 0 from `frontend/` — but the gate is still only run by hand. Until it is wired into CI alongside `make test-frontend`, the same class of error can accumulate again unseen. |
| D60 | Behaviour | [voice.md](voice.md#voice-messages) | `VoiceMessagePlayer` renders no waveform and derives a voice message's length from the audio file rather than from the timeline metadata that accompanies it. `chat-message-body.tsx` used to pass `durationMs` and `waveformPeaks` to a component that declared neither prop, so the values were dropped silently and the mismatch was one of D34's three type errors; the props were removed rather than wired up, because building a waveform is a chat feature, not a theming one. The player shows `--:--` until the file has loaded enough to report a duration. |
| D37 | Environment | [workspace-navigation.md](workspace-navigation.md#mobile-application) | Maestro cannot drive a physical iPhone here, so the mobile flows are verified on a real Android device and an iOS *simulator*. Maestro 2.3.0's `test` does not enumerate connected iPhones at all; 2.8.0 and 2.10.0 do, but fail to build their XCUITest driver — the `MaestroDriverLib` sources they extract are missing, so `xcodebuild` stops on absent input files, with or without `-allowProvisioningUpdates`. Detection also needs the CoreDevice tunnel warm (`xcrun devicectl device info details --device <udid>` immediately beforehand), which is a second, separate obstacle. Until an upstream release fixes the driver bundle, iOS-only regressions that a simulator cannot show — anything touching real signing, push, or hardware — go unseen by the blackbox suite. |
| D39 | Contract | [workspace-navigation.md](workspace-navigation.md#feature-tour) | FR-013a describes the tour's ritual stop falling back to project creation "when the workspace has no project yet", but organization registration seeds a default project (`internal/organization/logic.go`), so a freshly registered workspace always has one. The fallback is still reachable — default-project creation is allowed to fail without failing registration, and a workspace can archive its last project — and both branches are covered in `apps/web/e2e/feature-tour.spec.ts`, but the ordinary new-workspace path takes the non-fallback branch, not the one the requirement is written around. |
| D42 | Contract | [platform.md](platform.md#testing) | `ritual-ux-redesign.spec.ts`'s "if a board is available it is not the default route" assertion builds its pattern with `new RegExp('/workspace/tasks/.+\?view=review')`. In a single-quoted string `\?` is just `?`, so the compiled pattern is `.+?view=review` — a lazy quantifier followed by a literal `view=review`, not an escaped query separator. The negative assertion still passes for the right reason today, but it is looser than it reads and would not catch a URL that reached `view=review` by another path. Pre-existing; found while extending the same spec for feature 040 and left alone rather than silently changing an unrelated assertion's meaning. |
| D43 | Contract | [workspace-navigation.md](workspace-navigation.md#known-drift) | Under expo-router 55 a `Pressable` nested in `<Link asChild>` renders with **no style** when its `style` prop is a function (`({ pressed }) => [...]`); a plain style object works. Found on a device while verifying the review queue's tasks-tab entry card, which shipped unstyled and was switched to a plain object. `ProjectRow`, `FocusTaskRow` and `RitualFocusRow` in `app/(app)/(tasks)/index.tsx` still use the function form and are presumably affected identically; changing them was out of scope for feature 041. The props typecheck, so nothing catches this but looking at the screen. |
| D44 | Contract | [workspace-navigation.md](workspace-navigation.md#mobile-application) | `isUUID` in `apps/mobile/src/hooks/use-resolved-project-id.ts` pins the UUID version nibble to `[1-5]` and the variant to `[89ab]`, but this repository issues UUIDv7. Every real id therefore fails the test and is treated as a slug to be resolved, so the hook issues a `ListProjects` round trip for ids that needed none. Nothing breaks — the route still works — which is why it has gone unnoticed. The same shape check in `app/(app)/(more)/docs/[slug].tsx` is deliberately version-agnostic; this copy was left alone as out of scope for feature 046. |
| D57 | Behaviour | [workspace-navigation.md](workspace-navigation.md#known-drift) | Mobile email sign-in bounces back to the workspace picker on the **iOS simulator** for an owner account created through `RegisterOrganizationWithAdminPassword`, sometimes after "Your session is no longer valid". The identical build and credentials sign in on Android, and `Login`, `GetProfile` and `GetOrganizationBySubdomain` all succeed against the backend directly, so the fault is in the iOS sign-in screen's token-then-profile sequence rather than in the API. It prevents arranging a populated mobile fixture on iOS. |
| D50 | Environment | [workspace-navigation.md](workspace-navigation.md#known-drift) | The Expo dev client's floating **Tools button** on Android sits over the top-right of every screen and swallows taps meant for the header action beneath it — `task-mode-toggle` on the tasks tab in particular — opening the dev menu instead. It is on by default in a freshly installed debug build, so a Maestro run against a new emulator fails on header actions until it is switched off in the dev menu. Not a product bug and not reproducible in a release build; recorded because it costs an hour to diagnose from the failure message, which reads as a missing element. Switching it off does not stick: every Maestro flow relaunches the app, and the dev client restores the toggle, so `screens/today.yaml`'s Schedule handoff (`tab-calendar` -> `add-event-button`) cannot pass locally on the emulator at all. Confirmed against the pre-feature-047 version of that flow, which fails on the identical step. |
| D51 | Contract | [workspace-navigation.md](workspace-navigation.md#mobile-application) | Three existing mobile flows (`ritual-submission-flow.yaml`, `ritual-procedure-doc.yaml`, `tasks/evidence-review-approve.yaml`) wait on `assertVisible: "Focus"` after opening the tasks tab. The tab root has been titled **My Work** since the focus/projects mode split, so the assertion cannot pass. Feature 044's new flows wait on `task-mode-toggle` instead; the three older ones were left alone rather than silently changing assertions in unrelated flows, and they are unrunnable anyway while D40 stands. |
| D52 | Environment | [platform.md](platform.md#testing) | `TestEvidenceReviewQueuePerformance/.../the first page returns in under 2 s at p95` fails under full-suite load and passes in isolation, so `make test-backend` is not green on a clean tree. Under `go test -parallel` with the whole integration package running, the first `ListEvidenceReviewQueue` page measured p50=3.83s p95=4.53s against a 2s budget; run alone the whole test finishes in 6.4s. The budget is a wall-clock assertion with no isolation from the other tests contending for the same local Postgres, so it measures the machine's load as much as the query. Unrelated to feature 044, which touches nothing in the review queue. Fixing it means either measuring the query rather than the round trip, or marking the test as one that must not share a database. |
| D53 | Contract | [workspace-navigation.md](workspace-navigation.md#federated-search) | `internal/linking` emits `/workspace/calendar/{eventId}` as the canonical web route for a calendar event, but `apps/web` has no such page — only `/workspace/calendar`. A canonical event link therefore 404s on web, and the federated-search Event row deliberately opens the calendar itself instead of the route the linking service would produce. Found while wiring feature 045's Event row; not fixed there, because building a web event detail page is a calendar change in its own right. Mobile has `(calendar)/[eventId]` and is unaffected. Feature 046 inherits it: an event preview card names the event correctly but, on web, lands on the `/workspace/calendar` fallback rather than the event. Still open. |
| D54 | Environment | [platform.md](platform.md#testing) | `make test-frontend` is not green on a clean tree. Under full-suite load four specs fail that pass when run directly: `canonical-resource-links.spec.ts` "the browser lands on the correct web task destination", `legal-surface.spec.ts` "they cannot proceed without acknowledging the terms", `ritual-submission-flow.spec.ts` "a dual-role owner still sees both proof submission and review controls", and `user-guide-screenshots.spec.ts` "sign-in screens". The first is a harness artifact: a cross-document navigation that catches the Next dev server mid-compile receives its "missing required error components" placeholder instead of the page, which is also why `chat-link-previews.spec.ts` asserts the preview card's `href` and navigates rather than clicking it. The other three were not investigated. |
| D41 | Environment | [platform.md](platform.md#testing) | Three web E2E specs fail against a local backend independently of any feature work, so `make test-frontend` is not green on a clean tree: `context-rail.spec.ts:155` (the rail's live global blocks on the calendar route), and `legal-surface.spec.ts:59` and `user-guide-screenshots.spec.ts:626` (both fail in setup: `RegisterOrganizationWithAdminPassword` refuses the terms version the fixtures send with "those are not the terms currently in force", so neither test reaches its assertion). Verified by reverting `frontend/apps/web` entirely and re-running them: all still fail. A fourth, `voice-communication.spec.ts:157` (the decline was not reflected in the caller's timeline), was listed here until feature 052 fixed it: the decline's terminal event was wiping the caller's own timeline state, and the whole `voice-communication` spec now passes. A fifth, `ritual-ux-redesign.spec.ts:375`, fails only under full-suite load and passes in isolation. |

| D45 | Environment | [platform.md](platform.md#testing) | `ritual-ux-redesign.spec.ts:486` ("Today keeps standard tasks and ritual runs in separate labeled sections") fails for part of every day, wherever the machine's local timezone is ahead of UTC. `api.setStandardTaskDueToday` writes `CURRENT_DATE + interval '6 hour'` in Postgres, whose server timezone is `Etc/UTC`, while `TodayView` computes "today" from the browser's **local** midnight. On a UTC+7 machine the two disagree from 17:00 local until midnight, so the task the fixture just marked due today is filtered out as yesterday's. Reproduced in isolation at 04:00 local (UTC 21:00 the previous day) and diagnosed against `TodayView.tsx:255`; unrelated to feature 042, which does not touch the Today view or the mixed overview. The fix is for the fixture and the view to agree on one clock — either write the due date in the browser's timezone or have the view compare in UTC. |

| D46 | Behaviour | [calendar.md](calendar.md#shift-coverage) | Shift coverage excludes attendees with `role = 'organizer'`, so a manager who genuinely works a shift they published is not seen as covering it and cannot be drawn by an on-shift ritual pool. The exclusion is deliberate and necessary — `CreateEvent` writes the acting user as the organiser attendee, and that actor is normally the manager publishing the rota, so counting the row would roster them onto every shift — but the schema offers no way to distinguish "organised it" from "organised it and is working it". Fixing it properly means letting an event carry the organiser as a second, ordinary attendee row, which is a calendar change, not a ritual one. |
| D47 | Environment | [platform.md](platform.md#testing) | A `techoffice://` deep link cold-starts the Expo dev client rather than routing inside the running app, and the restarted app lands on the sign-in screen with its session gone. This makes the notification-tap path (`app/(shared)/resource/...`) unverifiable by deep link on a dev build; it has to be reached by navigating in-app instead. Found while verifying the feature 042 banner on the Android emulator. Unknown whether a release build behaves the same way. |
| D48 | Contract | [rituals-tasks.md](rituals-tasks.md#the-procedure-document-feature-043) | Spec 043 expected the ritual procedure entry point to be a canonical resource link (feature 030). It is not: it is an in-product overlay on every surface. A canonical link resolves to a *route*, and there is no route that can render an attached document for a reader who has no `docs.document_access` grant on it — the whole point of the feature is that such a reader can read it. Routing them at the docs viewer would refuse them; routing them at a new collaboration-owned document route would recreate the overlay as a page and lose the in-progress evidence capture underneath it, which FR-013 and FR-014 exist to prevent. The document remains canonically linkable in the ordinary way for anyone who *does* have access; only the ritual's own entry point is not a link. |
| D49 | Behaviour | [docs-knowledge.md](docs-knowledge.md#access-control) | The document **tree listings** (`ListRootDocuments`, `ListChildDocuments`) are **organization-scoped, not access-scoped**: they return every non-deleted workspace document in the organization to any caller holding `docs.view`, with no `docs.document_access` predicate, so document titles are visible in the docs sidebar more widely than document content. Per-document access is enforced from `GetDocument` downwards. Feature 045 closed the search half of this — `SearchDocuments` now carries the full owner → employee-grant → department-grant → visibility predicate for every caller — but deliberately left the tree alone, because scoping the sidebar changes what every person sees in it and is a change in its own right. |
| D58 | Contract | [rituals-tasks.md](rituals-tasks.md#review-queue) | `ListEvidenceReviewQueue` joins `organization.employee` directly to resolve the submitter's display name, which Constitution IV forbids: the collaboration → organization edge is supposed to cross through an interface, not a SQL join. Pre-existing. Feature 047's `ListTeamAttentionItems` faced the identical "one name per row, one lookup per page" problem and deliberately used the existing `EmployeeNameLookup` interface instead of copying the nearby join — a direct join would also have to be a `LEFT JOIN` to avoid dropping a row whose assignee has departed, which is the behaviour the feature requires. Recorded rather than silently repeated, because copying a violation because it is nearby is how drift becomes the norm. Closing it means moving the review queue's name resolution to the same batched lookup.
| D61 | Behaviour | [organization-people.md](organization-people.md#listing-and-cards) | `SearchEmployees`' email leg (`e.email % query`) is a sequential scan: `organization.employee` carries trigram indexes on `given_name` and `family_name` only, while `idx_identity_email_trgm` covers `iam.identity.email`, which that query no longer reads — the employee email moved to `organization.employee`. Nothing is wrong with the results; the leg just gets no index. Found while checking feature 048's decision to delegate directory narrowing to this matcher, and deliberately not fixed there: adding an index is a migration, and the feature was scoped to add none. Closing it is one `gin (email gin_trgm_ops)` index on `organization.employee`. |
| D62 | Behaviour | [workspace-navigation.md](workspace-navigation.md#federated-search) | The `WITH search_config AS (SELECT set_limit(0.1))` + `CROSS JOIN search_config` idiom in `SearchEmployees`, `SearchDepartments` and friends does **not** lower the trigram threshold for the `%` operator in the same statement: PostgreSQL does not guarantee the CTE is evaluated before the qualifier that depends on the GUC, and in practice it is not. `pg_trgm.similarity_threshold` therefore stays at its default 0.3, so the documented "lower threshold for short queries" is not in force. Verified directly: a department whose `name \|\| ' ' \|\| description` scores `similarity = 0.25` against its own name returns no row from the `%` predicate, while `similarity()` reports the 0.25. The practical effect is that queries diluted by a long concatenated haystack — a department with a description, an employee matched on a long email — silently miss. Found while seeding feature 048's Maestro department fixture; not fixed there, because the right fix is either `SET pg_trgm.similarity_threshold` on the connection or replacing `%` with an explicit `similarity(...) >= 0.1`, and both change the result set of every existing search. |
| D64 | Environment | [compliance-safety.md](compliance-safety.md#store-manifest) | `check-store-manifest` fails at HEAD, so `make test-mobile` aborts before running a single flow — the Maestro suite can only be reached by invoking `frontend/apps/mobile/scripts/run-maestro-suite.sh` directly. Five problems: `app.json` omits `NSFaceIDUsageDescription`, and the committed `ios/TechOffice/Info.plist` declares `NSLocationAlwaysUsageDescription`, `NSLocationAlwaysAndWhenInUseUsageDescription`, `NSLocalNetworkUsageDescription` and `NSBonjourServices`, all of which the checker says must not ship. The last two are what the Expo dev client and LiveKit need to find Metro on the LAN, so they arrived when the prebuilt `ios/` tree was committed; scrubbing them by hand would break local development, and the real fix is to keep the dev-only keys out of the committed prebuild. `Info.plist` and `app.json` are both clean and were last changed well before feature 048, which added no permission and no dependency. |
| D65 | Environment | [platform.md](platform.md#known-drift) | `TestPDFConversion`'s "polling eventually yields completed or pending" is refused with `permission_denied: insufficient permissions` on some full `make test-backend` runs. It passes when run alone and passed on an immediate repeat of the full suite, so it is load-dependent — but the symptom is a permission decision rather than the latency budget that makes `TestEvidenceReviewQueuePerformance` flaky, so it may not have the same cause. Found while running the suite as feature 049's regression guard and not investigated there — 049 adds no files or permission surface. |
| D59 | Behaviour | [workspace-navigation.md](workspace-navigation.md#known-drift) | Today's event sort crashes the whole screen with `Render Error: _left$startTime.getTime is not a function` when a `CalendarEvent.startTime` is truthy but not a `Date`. The line is `(left.startTime?.getTime() ?? 0) - (right.startTime?.getTime() ?? 0)` — optional chaining guards `null`, not a wrong type, so a string date passes the guard and then throws. Seen once on the Android emulator while verifying feature 047, after a bare `launchApp` rather than the suite's normal bootstrap; not reproducible through `auth/signin.yaml`, and the line is byte-identical to its pre-047 version, so it is pre-existing rather than a regression. It matters because it takes down the entire Today screen, including the personal sections a worker depends on — the one failure mode feature 047 went out of its way to make impossible for the Team block. Fixing it means coercing the event times at the API-wrapper boundary rather than trusting the field's declared type.
| D66 | Behaviour | [workspace-navigation.md](workspace-navigation.md#mobile-application) | Signing out does not fully empty the persisted React Query cache: `profile`, `userProfile` and `employee-permissions` survive `resetAuthenticatedAppState`, still carrying the signed-out person's employee id. Verified at the storage level on both platforms by dumping `mmkv/rq-cache` before and after a real sign-out — every other key (`projects`, `recentChannels`, `unread-count`, `user-preference`, `notification-preferences`) is correctly gone. The cause is that `resetAuthenticatedAppState` clears the cache and the MMKV blob, but the queries that lack an `enabled: isAuthenticated` guard refetch immediately afterwards and the cache subscriber re-persists them; `notification-preferences` is purged precisely because it has that guard. Found while proving feature 051's US1 scenario 2 (a colleague on the same handset sees their own values), which is unaffected — the notification preferences are purged, and the server derives the person from the session regardless. Recorded rather than fixed because the remedy is to add the same `enabled` guard to three other domains' queries, which was outside 051's scope. |
| D68 | Behaviour | [notifications-presence.md](notifications-presence.md#presence-the-ping-pong-protocol-feature-033), [voice.md](voice.md#four-client-rules-the-call-surfaces-depend-on) | A channel-scoped live event reaches only connections whose `active_connection.active_channel_id` already names that channel, and the clients report the channel they are viewing **on a presence pong**, which the server drives on its ping interval. Someone who has just opened a conversation is therefore invisible to channel-scoped delivery for up to one ping — long enough that a caller who opens a conversation and immediately places a call misses the `voice_call_ended` event for their own attempt. Found while making feature 052's "the entry appears without a reload" scenario deterministic; worked around there by having both clients refresh the transcript directly when the call is refused, which is the reliable path for the caller's own action but does nothing for any other channel-scoped event. Closing it means reporting the active channel on navigation rather than waiting for the next pong. |
| D69 | Behaviour | [chat.md](chat.md#channels) | There is no read-only way to ask how many messages a member has not seen. `MarkChannelAsRead` is the only RPC that returns the number and it clears the state as it reports it, `ListChannels` does not populate `ChannelMembership.unread_count`, and the web channel list derives its unread marks client-side from the live notification stream. The consequence is that someone who was offline when a conversation changed comes back to a channel list showing nothing unread, even though `GetUnreadMessageCount`'s predicate says otherwise — which is exactly the case feature 052 creates for a callee who could not be reached. The backend behaviour is correct; only the surfaces are missing. Found while writing 052's FR-008 coverage, which asserts the count in the backend scenario against `GetUnreadMessageCount`'s own predicate because no client-visible surface exists to assert against. Closing it means populating unread on `ListChannels` and having the sidebar read it. |
| D67 | Environment | [workspace-navigation.md](workspace-navigation.md#mobile-application) | `pnpm lint` fails at HEAD with 177 errors and 1 warning across 78 files in all three workspaces (`apps/mobile`, `apps/web`, `packages/apis`, `packages/notifications`, `scripts/`), so no feature can honestly tick a "lint clean" box and the command gives no signal about the change under review. The errors are the ordinary accumulated kind — `no-unused-vars` on dead imports, `no-explicit-any`, `no-empty`, and `no-undef` on `process`/`Buffer` in Node scripts that are being linted with browser globals. Confirmed pre-existing rather than introduced by feature 051 by stashing the entire working tree and re-running: the clean tree fails identically. Nothing runs `pnpm lint` in CI, which is why it drifted this far, and it is the same gap as D34 (`typecheck:mobile`) one command over. Until it is either fixed and wired into CI or narrowed to changed files, the practical substitute is running `npx eslint` on the files a change touches, which is what 051 did — all seven of its files exit 0. |

### Fixed on 2026-08-30

D1 (`upload_context` CHECK), D2 (Go notification-type list vs DB CHECK, now guarded by
`TestNotificationTypeCheckMatchesGoConstants`), D3 (dead ritual notification code and its
CHECK values), D4 (`expo-local-authentication` and its permissions), D6 (Zitadel residue:
`LoginForm.tsx`, `organization.project_id` / `app_id`), D7 (phantom notification types in
the mobile route resolver), D9 (calendar reminder priority and body), D10
(`document_type` filtering in the docs list), D11 (employee UUIDs rendered where a name
belongs), D12 (IP-literal hostnames and the `OrgSelector` mount effect), D13 (one password
rule at 8 characters, and the signup submit button's dead first click), D14 (`CreateProject`
returning a stale `member_count`), D15 (the auto-approve label), D19 (`AcceptInvitation`
and second-organization membership), D20 (`TestPresencePingPong` racing the rescue worker),
D25 (no in-app mute), D26 (decline indistinguishable from cancel). D8 was already resolved
when the register was written: `schema.sql` is generated from the migrations by
`backend/scripts/regen-schema.sh` and can no longer disagree with them.

### Fixed on 2026-09-02

D31 and D32, the two deferred halves of feature 038. The message→task chip
(`ListTasksBySourceMessages`), the task origin block (`GetTaskOrigin`) and the channel's
remembered destination (`GetChannelTaskDestination`, `SetChannelTaskDestination`) all have
logic-layer implementations, Connect handlers and callers on both clients now.

### Fixed on 2026-09-03

D29. Feature 040 makes `overdue` and `missed` real stored states. A second platform-wide
job, `ritual_reconciliation_sweep`, runs every five minutes, discovers organizations through
late *instances* rather than active definitions, and drives the same
`reconcileRitualTaskStateForTask` writer the evidence path uses — so the two paths cannot
diverge. `ritual_instance_overdue` and `ritual_instance_missed` went back into the Go
constant list and the DB CHECK in one change set, which
`TestNotificationTypeCheckMatchesGoConstants` enforces.

### Changed on 2026-09-04

Feature 042 adds a third department-pool assignment strategy, `on_shift`, and with it a
third platform-wide collaboration job, `ritual_shift_resolution_sweep`, on a two-minute
cadence. Because ritual instances are materialised 30 days ahead and shift rotas are
published a week or two ahead, the strategy is resolved late: generation records a
`collaboration.ritual_instance_pool_assignment` row per (instance, pool) and the sweep
binds, rebinds and finally escalates it. Rituals read the rota through
`collaboration.ShiftCoverageReader`, an interface collaboration declares and `calendar.Logic`
implements, injected by a setter in `cmd/server.go` — so the runtime call points
collaboration → calendar while the import graph still points only calendar → collaboration.
See [rituals-tasks.md](rituals-tasks.md#on-shift-assignment-feature-042),
[calendar.md](calendar.md#shift-coverage) and `backend/docs/SYSTEM-ARCHITECTURE.md` §7.

There is deliberately **no** fallback from `on_shift` to another strategy. A pool whose date
has nobody rostered leaves the instance unassigned and says so, which is the whole point:
falling back to round-robin is what put the closing checklist on somebody who was not in.

### Fixed on 2026-09-04

D38. Feature 044 gives the mobile app the two create surfaces whose absence made the
administrator tour's `project` and `ritual` stops web-only: `(app)/(tasks)/create-project`
and `(app)/(tasks)/[projectId]/create-ritual`. With somewhere real to land, `WebOnly` was
cleared on both stops in `internal/tour/content.go` and their `MobileNote` copy deleted, so
the tour now offers an action on mobile for both. Only the `people` stop remains web-only.
Constitution principle XIII was amended to v5.20.0 in the same change set to admit project
and ritual-definition creation as mobile capabilities; role and permission editing,
department management, bulk member import, account deactivation, credential reset for
others, and billing and quota management stay web-only.

## Keeping these current

The trigger is behaviour change, not spec authorship. Update the relevant domain document
in the same change set that alters an RPC surface, a database constraint, a background job
cadence, or a cross-domain call — the same Definition of Done that Constitution principle
XII applies to `backend/docs/`. When you fix an entry in the drift register, delete the
row rather than annotating it.
